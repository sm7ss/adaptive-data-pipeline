data:
  input_path: 'data/megaline_calls.csv'
  output_path: 'processed.parquet'

clean_data: 
  operation: 'drop_nulls'
  col: 'duration'

window_data: 
  operation: 'rolling_mean'
  col: 'duration'
  window: 5
  over: 'user_id'

agg_data: 
  operation: 'mean'
  col: 'duration'
  group_by: 'user_id'

join_data: 
  join_on: 'user_id'
  join_type: 'left'
  post_filter: 
    operations: ['duration < 45']


